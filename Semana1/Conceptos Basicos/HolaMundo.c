#include <stdio.h> // genera la salida estándar en stdio.h

int main() { //archivo de cabezera
    printf("Hola, Mundo!\n"); // lleva ; para finalizar la instrucción
    return 0; //esta declaracion finaliza la funcion main
}

//El número 0 generalmente significa que nuestro programa se ha ejecutado correctamente. Cualquier otro número indica que el programa ha fallado.